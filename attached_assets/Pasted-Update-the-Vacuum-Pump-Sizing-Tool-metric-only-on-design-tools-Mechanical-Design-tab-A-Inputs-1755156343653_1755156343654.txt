Update the Vacuum Pump Sizing Tool (metric-only) on /design-tools → Mechanical Design tab.
A. Inputs (add these fields)

Volume V (m³)

Start pressure p₁ (mbar abs)

Target pressure p₂ (mbar abs)

Evacuation time t (min)

Gas/Vapor load G (mbar·L/s) (optional)

Operating pressure p_oper (mbar abs)

Line conductance C (L/s) (optional)

Safety factor slider 1.0–2.0 (default 1.3)
Keep existing Gas Type and Temperature; keep metric only.

B. Calculations (implement exactly)

Time-based speed 
𝑆
𝑡
=
𝑉
𝑡
s
e
c
ln
⁡
 ⁣
𝑝
1
𝑝
2
S
t
	​

=
t
sec
	​

V
	​

ln
p
2
	​

p
1
	​

	​

 (compute in m³/s, display L/s and m³/h).

Holding requirement 
𝑆
hold
=
𝐺
𝑝
oper
S
hold
	​

=
p
oper
	​

G
	​

 (L/s; if G blank → 0).

Required effective speed at chamber 
𝑆
eff,req
=
max
⁡
(
𝑆
𝑡
,
𝑆
hold
)
×
safety
S
eff,req
	​

=max(S
t
	​

,S
hold
	​

)×safety.

Pump speed accounting for conductance

If C provided: 
𝑆
pump,req
=
1
1
𝑆
eff,req
−
1
𝐶
S
pump,req
	​

=
S
eff,req
	​

1
	​

−
C
1
	​

1
	​

.

If denominator ≤0 → show warning “Conductance too low—enlarge foreline or add port(s).”

Power (rough): 
𝑃
≈
𝑄
𝑠
 
[
m
3
/
s
]
⋅
Δ
𝑝
 
[
P
a
]
𝜂
P≈
η
Q
s
	​

[m
3
/s]⋅Δp[Pa]
	​

 with 
𝑄
𝑠
=
𝑆
pump,req
/
1000
Q
s
	​

=S
pump,req
	​

/1000 and 
Δ
𝑝
Δp taken at worst band (default 50 kPa) and 
𝜂
=
0.6
η=0.6 unless user overrides.

Pump-down time (display): compute from constant 
𝑆
eff,req
S
eff,req
	​

 as baseline; if a curve is uploaded (later), show both.

C. UI (labels & outputs)

Replace “Required Flow Rate” with “Required Pumping Speed (S)”.

Results card must show:

Sₜ (L/s, m³/h)

S_hold (L/s)

S_eff,req (L/s, m³/h)

S_pump,req accounting for C (L/s, m³/h)

Estimated Power (kW)

Safety factor (editable)

Keep “Recommended Pump Type” but compute as:

𝑝
2
≥
1
 
mbar
p
2
	​

≥1mbar & clean/dry → Rotary vane / dry scroll/screw

𝑝
2
<
1
 
mbar
p
2
	​

<1mbar → Add Roots + backing

𝑝
2
≤
1
×
10
−
3
 
mbar
p
2
	​

≤1×10
−3
mbar → Turbo + dry backing

If Gas Type is Water/solvent → suggest gas ballast or pre-condenser; add warning.

D. Warnings

If 
𝑝
2
≥
𝑝
1
p
2
	​

≥p
1
	​

 or any non-positive input → validation error.

If C causes 
𝑆
pump
→
∞
S
pump
	​

→∞ → conductance warning.

If Gas Type is “Water/solvent” and 
𝑇
T near dew point → show condensation / gas-ballast note.