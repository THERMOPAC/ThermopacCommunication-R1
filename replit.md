# Overview
This project is an enterprise-grade Quality Management System (QMS) for THERMOPAC, designed to optimize operations, enhance efficiency, and provide data-driven insights. It integrates core business modules such as project management, production, quality control, finance, HR, and document management. The system aims to facilitate data-driven decision-making, offer advanced sales and marketing features including AI-powered lead generation and Google Ads integration, provide a global re-refining opportunity radar, and incorporate a multi-agent intelligence layer for proactive business management.

# User Preferences
Preferred communication style: Simple, everyday language.

# Global Date Standard
## Display Format
| Context | Format |
|---|---|
| UI / Tables / Reports / Exports | DD/MM/YYYY |
| Date-Time displays | DD/MM/YYYY HH:MM |
| Database storage | yyyy-MM-dd |
| HTML `input[type="date"]` value (internal) | yyyy-MM-dd |

## Shared Utility
- File: `client/src/lib/date-format.ts`
- `fmtDate(date)` → DD/MM/YYYY (returns `—` for null/invalid)
- `fmtDateTime(date)` → DD/MM/YYYY HH:MM

## Rules — Non-Negotiable
1. **No `toLocaleDateString()`** anywhere in the UI.
2. **No `format(date, 'MMM dd')` or similar non-DD/MM/YYYY patterns** for any date displayed to users.
3. **Always import and use `fmtDate` / `fmtDateTime`** from `@/lib/date-format` for every UI date display.
4. `format(date, 'yyyy-MM-dd')` is permitted only for API/DB calls (not display).
5. `format(date, 'HH:mm')` is permitted only for time-only (no date) display.
6. Month/year-only labels (e.g. `toLocaleDateString('en-IN', { month: 'short', year: 'numeric' })`) in statutory period columns are the only allowed exception — no day component means DD/MM/YYYY does not apply.
7. No mixed formats. One formatter, one standard, everywhere.

# System Architecture
## Core Architectural Decisions
The system is a full-stack web application employing organized, hierarchical data structures, standardized UI/UX components, robust data integrity, and real-time synchronization. It features role-based access control, comprehensive validation, and dialog-based editing with form pre-population. Google Cloud Storage (GCS) serves as the single source of truth for file metadata, with security managed via signed URLs and client-side caching for performance. UI/UX emphasizes consistent color schemes, card-based layouts, unified dropdowns, and branded professional report generation.

## Technical Implementations
- **Backend**: Express.js with TypeScript, PostgreSQL (Drizzle ORM), session-based authentication, Google Cloud Storage, and a RESTful API.
- **Frontend**: React with TypeScript, Wouter for routing, TanStack Query for state management, Radix UI components with Tailwind CSS, React Hook Form with Zod validation, and Vite for builds.
- **Error Handling**: Centralized, structured error handling on both backend and frontend, following a standard JSON format without exposing sensitive details.
- **Data Storage**: PostgreSQL on Neon, a dedicated GCS bucket for files, and database-backed sessions.
- **Key Modules & Features**:
    - **Project & Quality Management**: Manages projects, items, work orders, inspection orders, material identification, welder management, and welding procedures.
    - **Finance & HR Management**: Covers invoicing, payments, allocations, BRC tracking, GL Mapping, Statutory Compliance, TDS reconciliation, user management, attendance, DWAR, and an enhanced payroll engine. Attendance module uses IST-safe date utilities (`server/utils/date-ist.ts`): all "today" date derivations, duty-time DateTime construction, and the midnight scheduler are IST-aware. The midnight processor runs via `node-cron` at `0 0 * * *` Asia/Kolkata (IST midnight) with a startup catch-up pass for missed nights.
    - **Document Management**: Integrates with GCS for hierarchical storage, metadata tracking, templates, and access control. Includes a Drawing Registry with version control and CAD management, and EPC Document Attachment & Retrieval with GCS-backed storage, SHA-256 duplicate detection, and audit history. Features a DocType-based document control system for 21 EPC DocTypes with revision and supersession tracking.
    - **Sales & Marketing**: Includes AI-powered lead generation, product database, offer/quotation module, Google Ads integration, and Commercial Change Order (CCO) control with financial traceability and a commercial chain model.
    - **EPC Project Numbering & Sequence Engine**: Implements a structured project coding system (`{FY}-{NNN}`) and a concurrency-safe sequence engine for over 20 document types, ensuring unique identifiers and GCS path derivation.
    - **EPC Control Layers**: Enforces governance for design drawings (revision control via Drawing-Level ECR/ECN workflows), Bill of Materials (BOM lifecycle management), and project-scoped document numbering. Features a snapshot-based cancellation cascade for project status management and restoration.
    - **Commercial Pricing Layer**: Full margin/selling price system on top of cost roll-up. `projects` table gains `selling_currency`, `exchange_rate`, `exchange_rate_frozen_at`, `total_selling_price_inr`, `total_selling_price`, `incoterms`, `payment_terms`, `delivery_terms`, `offer_validity_days`, `default_margin_percent`. `project_items` gains `margin_percent`, `selling_price_inr`, `selling_price`, `pricing_locked_at`. New `project_commercial_snapshots` table for immutable versioned price sheets (JSONB item snapshot). Backend: 8 API endpoints — `GET /pricing`, `PATCH /pricing/terms`, `POST /pricing/exchange-rate/freeze`, `PATCH /pricing/items`, `POST /pricing/apply-default-margin`, `GET /pricing/snapshots`, `POST /pricing/snapshots`, `GET /pricing/snapshots/:id/price-sheet`. Formula: `selling_price_inr = rolled_up_cost × (1 + margin/100)`, `selling_price = selling_price_inr / exchange_rate`. Snapshot creation requires cost_lock_status = 'approved'. Frontend: "Pricing" tab in project detail with 3 sections — (1) Commercial Terms card (selling currency, exchange rate + freeze, default margin %, incoterms, offer validity, payment/delivery terms), (2) Item Margins table (per-item margin % input, live selling price preview, blended margin footer, "Apply default to all" button), (3) Commercial Snapshots table (version list with CSV price-sheet export). Cost-lock gate banner shown when costs not yet approved. Files: `server/project-routes.ts` (pricing routes ~line 2374), `shared/schema.ts` (projectCommercialSnapshots table), `client/src/components/project-detail-fixed.tsx` (Pricing tab + state/mutations).
    - **Test Data Flag System**: `is_test boolean DEFAULT false` columns exist on both `offers` and `projects` tables. All 18 existing offers and 12 existing projects are marked `is_test = true`. The DB trigger `trg_lock_confirmed_offer` was patched to allow `is_test`-only updates even on "Order Confirmed" offers. Backend: `GET /api/sales-marketing/offers?showTest=true`, `PATCH /api/sales-marketing/offers/:id/test-flag`, `GET /api/design/projects?showTest=true` (design-management-routes.ts), `GET /api/projects?showTest=true`, `PATCH /api/projects/:id/test-flag`. Storage: `getOffers(showTest)`, `getUserProjects(userId, showTest)`, `setOfferTestFlag`, `setProjectTestFlag`. Frontend: `client/src/hooks/use-test-data-toggle.ts` (localStorage-backed toggle hook), Offers page and Projects page both have: a Superuser-only amber "Show/Hide Test Data" toggle button, an amber info banner when test mode is active, amber "Test" badge on each test record, and a flask-icon button per row to mark/unmark individual records. Toggle state persists across page reloads via localStorage key `thermopac_show_test_data`. By default (toggle off) all test records are hidden from the UI and API responses.
    - **Product Hierarchy (Offers)**: Offer items support Parent→Child hierarchy (2-level max). `insertOfferItemsWithHierarchy` uses a two-pass db transaction (parents first, then children). `validateOfferItemHierarchy` enforces no self-parent, no child-of-child. `validatePreConversion` now includes hierarchy integrity checks (no orphan sub-items, no dangling parent_item_id, no nested sub-items) before Offer→Project conversion.
    - **Project Item Hierarchy**: Project items support unlimited-depth parent/child tree. `PATCH /api/project-items/:id/parent` enforces self-parent guard, cross-project guard, and cycle detection. Frontend tree renders with expand/collapse, depth indentation, CornerDownRight indicator, and a "Link as Sub-item" dialog. Offer→Project conversion preserves hierarchy via two-pass insert with `offerItemIdToProjectItemId` map. BOM explosion sets `parent_project_item_id` on created child items.
    - **BOM Cost Roll-up**: `project_items` table has `rolled_up_cost` (NUMERIC 14,2) and `rolled_up_at` columns. Roll-up algorithm: own cost = approved BOM `totalEstimatedCost × quantity` (else `estimatedCost`); parent with approved BOM whose children are all BOM-sourced uses BOM total only (no double-count); otherwise additive (parent own + SUM children). Two endpoints: `GET /api/projects/:id/cost-rollup` (on-demand, no DB write) and `POST /api/projects/:id/cost-rollup/freeze` (writes `rolledUpCost` back to each item). Frontend "BOM Cost Roll-up" panel in Project Details tab with "Compute Roll-up" and "Recalculate & Freeze" buttons, showing per-item cost basis badge (BOM approved / Estimated / No cost), own cost, rolled-up cost, and frozen cost columns.
    - **EPC Execution Plan (Unified Automation)**: Automates DO/WO/PO/IO draft generation and activation at Offer-to-Project conversion with a full-auto execution pipeline orchestrator, supporting both manual and full_auto modes. Includes automated triggers for BOM creation, dispatch readiness, and billing readiness.
    - **Multi-Agent Intelligence & Automation**: Features 11 agents with conflict control, an event bus, finding management, and audit logging, providing live EPC risks and dashboards.
    - **Employee Appraisal Module**: Supports hierarchical workflow, KPI/competency scoring, increment policies, and PDF report generation.
    - **Global Date Format Standard**: All date displays across the Administration module use DD/MM/YYYY. A shared utility (`client/src/lib/date-utils.ts`) exports `fmtDate()`, `fmtDateTime()`, `toIsoDate()`, `toDisplayDate()`, `april1Display()`, and `april1Iso()`. All admin pages (Payroll, Appraisals, Attendance, Leave, Visa, Legal, Meetings, Business Trips, Schengen, etc.) import and use this utility. Backend stores ISO; frontend converts for display only. Text inputs use DD/MM/YYYY with `toIsoDate()` parsing before API calls.
    - **Project Procurement Package Control (PPPC) — Phase 5 COMPLETE**: Full 6-phase procurement control module. Phase 0: buy-groups, buy-subgroups, uom-master seed + routes. Phase 1: BUY Package Catalog (`buy_packages`, `buy_package_lines`) with product family filter, group-adaptive technical attributes, flag badges (SEL/DS/INSP/CERT/COMP), activate/archive lifecycle. Phase 2: Project BUY Lists (`project_buy_list_headers`, `project_buy_list_lines`) copied from packages, draft→review→release→lock lifecycle, revision/supersession. Phase 3: Per-line item selection workflow (`buy_list_line_selections`) — master item picker, drawing fields, datasheet upload to GCS (SHA-256, revision seq, signed-URL path), approve/reject with reason. Phase 4: Raise PR — creates `project_items` (source='buy_list', tag_no) + `item_planning_records` (PLN number via doc-sequence-service), inline planning badge + re-raise for canceled/superseded PLN, collapsible Procurement Chain Status panel (PLN→Exec→PO Prep→PO→QC stage badges). Phase 5: Bulk operations + enhanced UI — `GET /api/pppc/buy-items` (buy master items picker), `POST /api/buy-lists/:id/bulk-select` (savepoints, per-line error isolation), `POST /api/buy-lists/:id/bulk-approve` (Manager+, savepoints), `POST /api/buy-lists/:id/bulk-raise-pr` (savepoints); per-line inline selection card (expandable row, master item picker, datasheet upload zone, approve/reject, Raise PR); line-level checkboxes + bulk toolbar (Bulk Select dialog, Bulk Approve, Bulk Raise PR); product family dropdown filter on BUY Package Catalog page. Auth: `ensureAuthenticated` + `requirePageAccess('buy-list-control')` on all routes. Key files: `server/pppc-routes.ts` (≈2100 lines), `client/src/pages/epc-buy-list-control-page.tsx`, `client/src/pages/buy-packages-page.tsx`.
    - **Alert Management System**: A comprehensive system with priority levels and a 3-state workflow.
    - **Security & Access Control**: Includes API security measures (SQL injection, XSS protection), TOTP-based 2FA, EPC Permission Control Dashboard (role-based, department-based, user-override), and EPC Record-Level Ownership Filtering.
    - **EPC Control Tower**: A program-level monitoring dashboard for project summaries, pipeline analysis, bottleneck identification, and risk indicators.
    - **EPC Cutover Readiness Dashboard**: Monitors migration progress, adoption of EPC features, and legacy system usage.
    - **Enterprise Document Control Framework (EDCF)**: Defines governed GCS roots, document types, and non-negotiable rules for document management, with an architecture for document registry, type registry, audit log, and access grants.
    - **GCS Governance Remediation (Rev 4 baseline — FINAL, Zero-Trust Compliant)**: Reference document: `docs/gcs-governance-rev4-closure.md`. All governed writes enforce `TPEL/{CC}/{CO}/{Cust}/{FY}/{NNN}/…` root (G1). Nine covered document families: DWG, DDS, QTN, INS, ECR, ECN, DSP, CO, TEMPLATE. Ten non-negotiable rules in force (G1–G10): TPEL root mandatory, one builder per family (`buildDrawingGcsPath`, `buildDdsGcsPath`, `buildEpcGcsPath`, `buildEpcQtnGcsPath`, `uploadTemplateToGcs`), `assertGcsPath()` called on every path, legacy prefixes (`QMS/`, `EPC/`, `THERMOPAC_PROJECTS/`, `engineering_changes/`) blocked by guardrail, G8 controlled vocabulary enforced with HTTP 422 on all upload routes, signed URLs only, ECR/ECN require `project_id` (HTTP 422 if absent), no silent fallback on geo resolution failure. Vocabulary source of truth: `shared/gcs-label-vocabulary.ts`. Zero-Trust audit confirmed: 287/287 `epc_document_attachments` rows at TPEL paths; zero non-TPEL paths in any governed table. Any new document flow must comply with `docs/gcs-governance-rev4-closure.md` — no deviations without formal review.
    - **Drawing Verification System (DVS)**: Controlled pipeline: Upload → Extract → Rule Engine → Agent → Approval → Release. Step 1 (Upload + Staging): `drawing_revisions` table, accepts `.slddrw` and `.pdf` files, SHA-256 checksum, GCS rollback on DB failure, immutable records, 409 on duplicates, STAGING zone enforcement, `file_type` column stores 'slddrw'|'pdf'. API at `/api/drawing-revisions`. Step 2 (Extraction Layer): `drawing_extractions` table (1:1 FK to `drawing_revisions`); dual-engine: (a) OLE engine — `cfb` npm, parses Compound Document binary, extracts document/custom/sheet properties; (b) PDF engine — `pdf-parse` npm (CJS via createRequire), regex-based title block parser from text layer. Engine routed by `revision.fileType`; `EXTRACTION_ENGINE`/`PDF_EXTRACTION_ENGINE` constants separate. Both engines return same `ExtractionResult` interface. `file_info` always populated; `validation_results` (DrawingNumber + Revision cross-check) always written; extraction trigger guard; version-aware re-extraction; failed retry requires `?force=true`; 30s timeout guard; idempotent; re-extraction does not roll back pipeline status past 'extracted'. Rule Engine: reads only from `drawing_extractions`, never raw files; `failed` status blocks evaluation. Routes: `POST /api/drawing-revisions/:id/extract`, `GET /api/drawing-revisions/:id/extraction`. Files: `server/utils/ole-extractor.ts`, `server/utils/pdf-extractor.ts`, `shared/schema.ts` (drawingExtractions table), `server/drawing-verification-routes.ts`.
    - **DDS PDF Generation System**: Design Data Sheet (DDS) auto-generates a PDF via puppeteer/Chromium on each POST/PUT save. PDF is uploaded to GCS under `TPEL/{CC}/{CO}/{Cust}/{FY}/{NNN}/{ItemCode}/DDS/{DrawingNumber}_dds-rev-{rev}.pdf` using `buildDdsGcsPath()`. DB columns: `dds_gcs_path varchar(500)`, `dds_pdf_status varchar(20)` on `design_data_sheets`. Fire-and-forget async generation (DB save never blocked). Status values: null (not yet attempted), 'ready', 'error'. Signed URL endpoint: `GET /api/drawing-design-data/:id/pdf-url`. Manual regeneration: `POST /api/drawing-design-data/:id/regenerate-pdf`. Frontend compact card replaces full inline preview; shows status badge (PDF Ready/Failed/Generating), last saved date, Download PDF / Preview PDF / Regenerate PDF buttons. HTML template: `server/dds-html-template.ts`. PDF service: `server/dds-pdf-service.ts`.
    - **SolidWorks Extraction Agent System (Phase 1 — COMPLETE)**: Full pipeline: Upload → Extract → DDS Comparison → Approval gate. Baseline: `docs/slddrw-extraction-agent-baseline-v3.md` (APPROVED). Architecture: Cloud app (Replit) handles workflow + DDS comparison; local Windows PC runs dedicated agent that polls cloud, downloads `.slddrw` via signed GCS URL, extracts data via SolidWorks COM API, uploads JSON. **Windows Agent** (`local-agent/`): Python 3.11 compiled to EXE via PyInstaller. Components: `agent/config.py` (config.ini loader + ProgID resolver), `agent/logger.py` (rotating file logger), `agent/job_client.py` (HTTP client with x-node-id/x-node-token auth), `agent/job_runner.py` (job orchestration + SHA-256 + job-level timeout via threading.Event), `agent/main.py` (poll loop + graceful shutdown). Extractor: `extractor/solidworks_extractor.py` (dedicated DispatchEx instance, never attaches to user session) + 10 modules: extract_properties, extract_sheets, extract_views, extract_dimensions, extract_annotations, extract_tables, extract_references, extract_health, extract_nozzles, extract_design_data (MANDATORY — hard failure if absent). Installer: `installer/setup.iss` (Inno Setup 6 → `ThermopacAgent-Setup-v1.0.exe`). **Cloud API** (`server/epc-slddrw-job-routes.ts`): GET /pending, POST /:id/claim (atomic), POST /:id/complete (Zod validated), POST /:id/fail, GET drawing-controls/:id/slddrw-jobs, POST /:id/retry, POST / (create). Per-node auth: `x-node-id` + `x-node-token` (bcrypt) validated against `epc_agent_nodes` table. **DDS Comparison Engine** (`server/utils/dds-comparison-engine.ts`): 10 parameters (5 critical, 5 warning), numeric normalisation via `drawing-unit-normalizer.ts`, outcomes: pass/warn/fail/blocked. fail/blocked blocks approval. **Tables**: `epc_agent_nodes`, `epc_slddrw_extraction_jobs` (jsonb extraction_result + dds_comparison_result). **Frontend**: `DrawingVerificationCard` component replaces Coming Soon placeholder at `/epc/drawing-controls`; live job status, DDS comparison results, auto-poll while pending/processing, retry button for admins. Preserved utilities: `drawing-rule-engine.ts`, `drawing-unit-normalizer.ts`.
    - **SolidWorks Agent v1.0.33 Update**: Design Data extraction is now best-effort. Missing or inaccessible Design Data tables no longer hard-fail jobs; the agent logs warnings, scans similar table titles and note/text candidates, returns `design_data.status`/`source`, and populates top-level `extraction_warnings[]`.
    - **SolidWorks Backend Validation Update**: `/api/epc-slddrw-jobs/:id/complete` accepts missing/empty `design_data_table`, normalizes Design Data status/source, returns/persists `extraction_warnings: ["Design Data table not found"]`, and treats missing structured Design Data as a DDS warning instead of a failed/blocked extraction.
    - **SolidWorks Agent v1.0.42 Dev Build**: Download ZIPs updated to v1.0.42 for Development testing. Corrects SolidWorks open option constants (`LoadModel=16`, `LoadLightweight=128`) and improves large drawing extraction depth by trying Detailing/RapidDraft mode before ViewOnly/Large Design Review, including a temp-copy editable detailing pass that still never saves. Extraction results now include `open_diagnostics`, and reference extraction falls back to SolidWorks document dependency APIs so large/view-only openings still report referenced model names.
    - **SolidWorks Agent v1.0.43 Pre-Opened Reference Test Build**: Download ZIPs updated to v1.0.43 for Development testing. Agent attaches to a running SolidWorks session when present, inventories dependency paths before drawing open, logs which referenced `.sldprt`/`.sldasm` files are already open, records whether full resolved open succeeds with those pre-opened dependencies, and writes `extraction_summary` / `open_diagnostics.post_open_extraction_summary` for model reference, dimensions, annotations, BOM, and Design Data comparison.
    - **SolidWorks Agent v1.0.44 Active Drawing Acceptance Fix**: Fixes the v1.0.43 failure where SolidWorks `ActivateDoc3` returned an already-open drawing object but the agent discarded it because the quick drawing API probe failed during open. The agent now accepts the activated drawing and proceeds to extraction, allowing the small-drawing Design Data proof run to complete when drawing/model are pre-opened.
    - **SolidWorks Agent v1.0.45 Design Data Table Fix**: Improves Design Data table extraction from the small drawing by reading table cells through multiple SolidWorks COM access patterns (`Text`, `Text2`, `DisplayedText`, and cell-text methods), exposing `design_data_table`, `extraction_summary`, and `open_diagnostics` in the JSON viewer, and canonicalizing drawing parameter names like "Internal Design Pressure" to DDS comparison fields.
    - **Extraction Result JSON Viewer**: Added `GET /api/extraction-results/:jobId` to expose stored SolidWorks extraction JSON by job, including job metadata/timestamps and extracted sections. Drawing Verification Card now includes a "View JSON" button with a pretty formatted JSON viewer.
    - **Agent Usage Tracker**: Local budget monitoring for Replit Agent Usage with configurable monthly/daily limits, warning thresholds at 50%/75%/90%/100%, soft-block messages, sidebar progress indicator (Superuser only), manual daily usage logging, and projected monthly cost display. Route: `/usage-tracker`, API: `/api/usage-tracker`.

# External Dependencies
- **Google Cloud Services**: Google Cloud Storage, Google Calendar API, Google OAuth 2.0, Google Custom Search JSON API.
- **Database Services**: Neon (PostgreSQL hosting).
- **Third-Party Libraries**: SendGrid, PDF-lib, Stripe, Radix UI, Lucide React, date-fns, TanStack Query, Wouter, React Hook Form, Zod, Vite, Drizzle ORM, Multer, bcrypt, nodemailer, ExcelJS (replaced vulnerable xlsx@0.18.5), jsPDF, chart.js, mssql, OpenAI GPT-4o, otpauth, jsonwebtoken, qrcode, express-rate-limit.