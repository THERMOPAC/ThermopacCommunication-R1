# ── Node / build ─────────────────────────────────────────────────────────────
node_modules
dist
.DS_Store
server/public
vite.config.ts.*

# ── Logs ──────────────────────────────────────────────────────────────────────
*.log
*.tar.gz

# ── Replit / local ────────────────────────────────────────────────────────────
attached_assets
temp
wpqr_documents

# ── Environment / secrets — NEVER commit these ───────────────────────────────
.env
.env.local
.env.production
.env.development
.env.*
!.env.example

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
**/__pycache__/
*.pyc
*.pyo
*.pyd
*.spec

# ── Agent build output (generated by build-installer.bat / GitHub Actions) ────
local-agent/dist/
local-agent/build/
local-agent/installer_output/

# ── Agent release ZIPs — only current version kept in git (client/public/) ──
# Old version ZIPs and throwaway bundles are excluded.
# The two active ZIPs for the current release are committed directly.
client/public/agent-files.zip
client/public/thermopac-agent-*.zip
client/public/ThermopacAgent-Phase1-Package.zip
client/public/ThermopacAgent-v1.0.[0-9].zip
client/public/ThermopacAgent-v1.0.1[0-9].zip
